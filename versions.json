{
  "registry": "ghcr.io/getbrik",
  "tools": {
    "yq": "v4.52.5",
    "jq": "1.8.1"
  },
  "security_tools": {
    "semgrep": "1.157.0",
    "osv_scanner": "2.3.5",
    "grype": "0.110.0",
    "syft": "1.42.4",
    "checkov": "3.2.517",
    "hadolint": "2.14.0",
    "license_finder": "7.2.1",
    "scancode_toolkit": "32.5.0",
    "gitleaks": "8.30.1",
    "trufflehog": "3.94.3",
    "dockle": "0.4.15"
  },
  "stacks": {
    "base": {
      "versions": ["3.23"],
      "image_template": "alpine:${VERSION}",
      "verify_cmd": "bash --version"
    },
    "node": {
      "versions": ["22", "24"],
      "image_template": "node:${VERSION}-slim",
      "verify_cmd": "node --version && npm --version"
    },
    "python": {
      "versions": ["3.13", "3.14"],
      "image_template": "python:${VERSION}-slim",
      "verify_cmd": "python3 --version && pip --version"
    },
    "java": {
      "versions": ["21", "25"],
      "image_template": "eclipse-temurin:${VERSION}-jdk-jammy",
      "extra_packages": ["maven"],
      "verify_cmd": "java --version && mvn --version"
    },
    "rust": {
      "versions": ["1"],
      "image_template": "rust:${VERSION}-slim-bookworm",
      "verify_cmd": "rustc --version && cargo --version"
    },
    "dotnet": {
      "versions": ["9.0", "10.0"],
      "image_template": "mcr.microsoft.com/dotnet/sdk:${VERSION}",
      "verify_cmd": "dotnet --version"
    },
    "quality": {
      "versions": ["1"],
      "image_template": "alpine:3.23",
      "verify_cmd": "semgrep --version && osv-scanner --version && grype version && syft version && checkov --version && hadolint --version && license_finder version && scancode --version"
    },
    "security": {
      "versions": ["1"],
      "image_template": "alpine:3.23",
      "verify_cmd": "gitleaks version && trufflehog --version && grype version && syft version && osv-scanner --version && dockle --version"
    }
  }
}
